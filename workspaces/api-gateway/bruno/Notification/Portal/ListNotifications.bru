meta {
  name: List Notifications
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/notifications?userId={{userId}}&limit={{limit}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}
